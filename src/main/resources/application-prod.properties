# =========================================================
# PRODUCTION PROFILE
# =========================================================

server.port=8080

# =========================================================
# MYSQL
# =========================================================

spring.datasource.url=jdbc:mysql://ias-academy.cp4e0acwc0xo.ap-south-1.rds.amazonaws.com:3306/academy_db?useSSL=true&serverTimezone=Asia/Kolkata
spring.datasource.username=academy
spring.datasource.password=SuTVVSRDNBzLLxPFxI6e
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =========================================================
# MAIL
# =========================================================

spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=587
spring.mail.username=db6f7d7123dc4b
spring.mail.password=3a7f8887a1fabc
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Academy contact
academy.contact.email=kwivrainnovations@gmail.com

# =========================================================
# JPA / HIBERNATE
# =========================================================

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

# =========================================================
# JWT (required — secret must be at least 32 characters)
# Prefer APP_JWT_SECRET on the live server. Do not set it to empty.
# =========================================================

app.jwt.secret=C0cX9KWgss2LLAiytN7A4C0rGd0xIHkR8cz3oDzddLmWHo/rPTCTucdx2ssWV298
app.jwt.access-token-expiration-ms=900000
app.jwt.refresh-token-expiration-ms=604800000

# =========================================================
# CORS
# =========================================================

app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://learnings.vibutharacademy.online}

# =========================================================
# IMAGE STORAGE
# =========================================================

app.images.storage-dir=images
app.images.url-prefix=/images

# =========================================================
# LOGGING
# =========================================================

logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.orm.jdbc.bind=WARN